const VideoLength = ({ videoLength }) => {
  return (
    <span className="absolute bottom-3 right-2 rounded-md bg-black px-1 text-center text-sm font-semibold text-white">
      {videoLength}
    </span>
  );
};

export default VideoLength;
















  